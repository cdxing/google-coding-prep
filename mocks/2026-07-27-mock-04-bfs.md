# Mock 04 — BFS (Multi-source)

## Problem
- LC 994: Rotting Oranges
- Pattern: Multi-source BFS, level-by-level

## Result
- Solved with guidance
- Score: 7/10

## What worked
- Immediately identified BFS as correct pattern
- Understood multi-source seeding (all rotten oranges at t=0)
- Correct use of `fresh` counter for -1 check

## Bugs
- `for r in rows` instead of `for r in range(rows)` — integer is not iterable
- `queue.top()` — does not exist in Python deque (should be `popleft()`)
- Missing bounds check on `r_new, c_new` before grid access
- Incremented `minute` per element instead of per level (missing `for _ in range(len(queue))` loop)

## Root cause
- API recall: deque has no `.top()`, use `popleft()`
- Loop structure: BFS level trick not yet muscle memory
- Bounds checking: not yet reflexive

## Follow-up
- LC 200 (Number of Islands, DFS counterpart) scaffold created
