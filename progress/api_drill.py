'''
API Recall Drill — write each 3 times from memory, no looking.

Target: zero hesitation on every line.
'''

# ============================================================
# 1. set
# ============================================================
# add element: set.add()
# remove element (error if missing): set.remove(element)
# remove element (safe): set.discard(element)
# check membership: if element in set:
# length: len(set)

# 2nd time:

# 3rd time:


# ============================================================
# 2. dict
# ============================================================
# set key: dict.setkey() -> dict[key] = value
# get with default: dict.get(key, default), dict[key]
# check key exists: if key in dict
# delete key: dict.delete(key) -> del dict[key]
# iterate keys: for key in dict:
# iterate key-value: for key, value in enumerate(dict): -> for key, value in dict.items()
# length: dict.size() -> len(dict)

# 2nd time:

# 3rd time:


# ============================================================
# 3. list
# ============================================================
# append to end: list.append()
# pop from end: list.pop()
# pop from index: list.pop(index)
# access last element: list[-1]
# access first element: list[0]
# length: len(list)
# sort in place: list.sort()
# sorted copy: sorted(list)

# 2nd time:

# 3rd time:


# ============================================================
# 4. deque  (from collections import deque)
# ============================================================
# create: queue = deque()
# enqueue (add to right): queue.push(element) -> queue.append(element)
# dequeue (remove from left): queue.popleft()
# peek front: queue[0]
# peek back: queue[-1]
# length: len(queue)

# 2nd time:

# 3rd time:


# ============================================================
# 5. heapq  (import heapq)
# ============================================================
# push: heapq.heappush(heap, element)
# pop smallest: heapq.heappop(heap)
# peek smallest: heap[0]
# heapify a list: heapq.heap(list) -> heapq.heapify(list)

# 2nd time:

# 3rd time:


# ============================================================
# 6. string (s)
# ============================================================
# length: size(s) -> len(s)
# access char at index: s[i]
# slice: s[i:j]
# check substring: if subs in s:
# split:  ? -> s.split(", "), s.split()
# join: fstring(s1 + s2 + 'string') -> ", ".join(list) or s1 + s2
# iterate: for c in s:

# 2nd time:

# 3rd time:


# ============================================================
# 7. common built-ins
# ============================================================
# max / min: max(x, y)/ min(x, y), maximum or miminum between x and y
# abs: abs(x), absolute value of x
# range(n): iterate from 0 to n -1
# range(start, stop): interate from start to (stop -1)
# range(start, stop, step): interate from start to  (stop - 1) with step size step
# enumerate: for i, num in enumerate(nums), where i is the index and num is the content
# zip: ? -> for x, y in zip(list1, list2):
# sorted with key: sorted(map) sorted(list, key = lamda x:x[1])
# sorted reverse: sorted(list, reverse=True)
# float infinity: float('inf')
# integer infinity: int('inf') -> float('inf')  or math.inf

# 2nd time:

# 3rd time:
