# Mock 03 — Heap / Top-K

## Problem
- LC 215: Kth Largest Element in an Array
- Pattern: Min-heap of size K

## Result
- Solved
- Score: 9/10

## What worked
- Wrote sort baseline first, then optimized to heap
- Correctly identified min-heap invariant (heap top = kth largest)
- Clean template structure

## Bugs
- `from collections import heapq` — wrong module (should be `import heapq`)
- `heapq.heapop()` — typo, missing `p` (should be `heappop`)

## Root cause
- API recall: import path and function name spelling

## Follow-up
- Pattern card written with kth-smallest (max-heap) and top-K frequent variants
